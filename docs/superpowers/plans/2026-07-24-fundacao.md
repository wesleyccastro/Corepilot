# CorePilot — Fase 1 (Fundação) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ter autenticação real (Supabase Auth), multi-tenant (Empresa/Usuario/Perfil) e auditoria funcionando de ponta a ponta no backend NestJS + frontend Vite, validado por um endpoint `GET /me` que nunca mistura dados entre empresas.

**Architecture:** Frontend usa `supabase-js` só para login (JWT); todo dado de negócio passa pelo NestJS, que valida o JWT via JWKS do Supabase (sem segredo compartilhado — confirmado: o projeto usa JWT Signing Keys/ES256), resolve o tenant explicitamente em cada request via um `TenantGuard` + `TenantContext` request-scoped, e fala com o Postgres do Supabase via Prisma usando uma connection string própria (sem RLS como mecanismo de autorização).

**Tech Stack:** NestJS 11, Prisma, `jose` (verificação JWT via JWKS), `@supabase/supabase-js` (Admin API para seed/testes), React 19 + Vite (frontend, sem test runner ainda).

## Global Constraints

- Lógica de backend só em `backend/` (NestJS) — nunca em Supabase Edge Functions (CLAUDE.md).
- Frontend nunca acessa o Postgres nem a API da Anthropic diretamente, e nunca recebe segredos (spec §3).
- Toda ação relevante é auditada: ator, timestamp, dados antes/depois (spec §4, `AuditLog`).
- Segredos (connection string, service role key) só em `backend/.env.local`, nunca commitados (já protegido pelo `.gitignore` da raiz, padrão `.env*.local`).
- Escopo de tenant é explícito no código de cada service (`where: { empresaId: ... }`), não implícito via RLS (spec §3, §5).
- Perfil é só `admin | membro` nesta fase — RBAC granular é Fase 8 (spec §2).
- Prettier do backend: aspas simples, trailing commas em tudo (`.prettierrc`) — rodar `npm run format` se em dúvida.
- Backend: testes Jest colocados junto do código (`*.spec.ts` em `src/`), e2e em `test/*.e2e-spec.ts` (`jest-e2e.json`, rootDir = `test/`).
- Frontend não tem test runner configurado ainda — não introduzir um nesta fase; verificação do frontend é manual (rodar `npm run dev` e testar no navegador).

---

## Task 1: Prisma — schema, migração e carregamento de ambiente

**Files:**
- Create: `backend/prisma/schema.prisma`
- Create: `backend/src/test-setup.ts`
- Create: `backend/src/prisma/prisma.smoke.spec.ts`
- Modify: `backend/package.json` (deps, jest `setupFiles`, scripts)

**Interfaces:**
- Produces: modelos Prisma `Empresa`, `Usuario`, `enum Perfil { admin, membro }`, `UsuarioEmpresa`, `AuditLog` (tipos gerados em `@prisma/client`); script `db:seed`, `prisma:migrate`, `prisma:studio`; carregamento automático de `backend/.env.local` em todo `npm test`/`npm run test:e2e` via `src/test-setup.ts`.

- [ ] **Step 1: Instalar dependências**

```bash
cd backend
npm install @prisma/client
npm install -D prisma dotenv dotenv-cli
```

- [ ] **Step 2: Criar o schema do Prisma**

Criar `backend/prisma/schema.prisma`:

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Empresa {
  id       String   @id @default(uuid())
  nome     String
  criadoEm DateTime @default(now())

  usuarios  UsuarioEmpresa[]
  auditLogs AuditLog[]
}

model Usuario {
  id             String   @id @default(uuid())
  supabaseUserId String   @unique
  nome           String
  email          String   @unique
  criadoEm       DateTime @default(now())

  empresas  UsuarioEmpresa[]
  auditLogs AuditLog[]
}

enum Perfil {
  admin
  membro
}

model UsuarioEmpresa {
  usuarioId String
  empresaId String
  perfil    Perfil

  usuario Usuario @relation(fields: [usuarioId], references: [id])
  empresa Empresa @relation(fields: [empresaId], references: [id])

  @@id([usuarioId, empresaId])
}

model AuditLog {
  id            String   @id @default(uuid())
  empresaId     String
  atorUsuarioId String
  acao          String
  dadosAntes    Json?
  dadosDepois   Json?
  timestamp     DateTime @default(now())

  empresa Empresa @relation(fields: [empresaId], references: [id])
  ator    Usuario @relation(fields: [atorUsuarioId], references: [id])
}
```

- [ ] **Step 3: Adicionar scripts e configuração de seed no `package.json`**

Em `backend/package.json`, dentro de `"scripts"`, adicionar:

```json
"prisma:migrate": "dotenv -e .env.local -- prisma migrate dev",
"prisma:generate": "prisma generate",
"prisma:studio": "dotenv -e .env.local -- prisma studio",
"db:seed": "dotenv -e .env.local -- prisma db seed"
```

E, no nível raiz do `package.json` (irmão de `"scripts"`), adicionar:

```json
"prisma": {
  "seed": "dotenv -e .env.local -- ts-node prisma/seed.ts"
}
```

- [ ] **Step 4: Rodar a migração contra o Postgres do Supabase**

```bash
npm run prisma:migrate -- --name init
```

Espera-se que crie as tabelas `Empresa`, `Usuario`, `UsuarioEmpresa`, `AuditLog` no banco e gere o client em `node_modules/@prisma/client`.

- [ ] **Step 5: Criar o carregador de ambiente para os testes Jest**

Criar `backend/src/test-setup.ts`:

```typescript
import * as dotenv from 'dotenv';
import * as path from 'path';

dotenv.config({ path: path.resolve(__dirname, '../.env.local') });
```

- [ ] **Step 6: Conectar o `test-setup.ts` ao Jest (unit e e2e)**

Em `backend/package.json`, dentro do bloco `"jest"` existente, adicionar:

```json
"setupFiles": ["<rootDir>/test-setup.ts"]
```

Em `backend/test/jest-e2e.json`, adicionar:

```json
"setupFiles": ["<rootDir>/../src/test-setup.ts"]
```

- [ ] **Step 7: Escrever o smoke test do Prisma (falha primeiro)**

Criar `backend/src/prisma/prisma.smoke.spec.ts`:

```typescript
import { PrismaClient } from '@prisma/client';

describe('Prisma smoke test', () => {
  const prisma = new PrismaClient();

  afterAll(async () => {
    await prisma.$disconnect();
  });

  it('conecta no Postgres e persiste uma Empresa', async () => {
    const empresa = await prisma.empresa.create({ data: { nome: 'Smoke Test Empresa' } });

    const encontrada = await prisma.empresa.findUnique({ where: { id: empresa.id } });
    expect(encontrada?.nome).toBe('Smoke Test Empresa');

    await prisma.empresa.delete({ where: { id: empresa.id } });
  });
});
```

- [ ] **Step 8: Rodar o teste e confirmar que passa**

Run: `npm test -- prisma.smoke.spec.ts`
Expected: PASS (1 teste), confirmando que `DATABASE_URL` está sendo carregado e a migração foi aplicada.

- [ ] **Step 9: Commit**

```bash
git add backend/prisma/schema.prisma backend/src/test-setup.ts backend/src/prisma/prisma.smoke.spec.ts backend/package.json backend/package-lock.json backend/test/jest-e2e.json
git commit -m "feat(backend): schema Prisma core e carregamento de .env.local nos testes"
```

---

## Task 2: PrismaService/PrismaModule e ConfigModule global

**Files:**
- Create: `backend/src/prisma/prisma.service.ts`
- Create: `backend/src/prisma/prisma.module.ts`
- Modify: `backend/src/app.module.ts`
- Modify: `backend/src/main.ts`

**Interfaces:**
- Consumes: `PrismaClient` (Task 1).
- Produces: `PrismaService` (injetável Nest, estende `PrismaClient`), `PrismaModule` (`@Global()`, exporta `PrismaService`) — usado por todas as tasks seguintes que precisam do Prisma.

- [ ] **Step 1: Instalar `@nestjs/config`**

```bash
cd backend
npm install @nestjs/config
```

- [ ] **Step 2: Criar `PrismaService`**

Criar `backend/src/prisma/prisma.service.ts`:

```typescript
import { Injectable, OnModuleDestroy, OnModuleInit } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
  async onModuleInit() {
    await this.$connect();
  }

  async onModuleDestroy() {
    await this.$disconnect();
  }
}
```

- [ ] **Step 3: Criar `PrismaModule`**

Criar `backend/src/prisma/prisma.module.ts`:

```typescript
import { Global, Module } from '@nestjs/common';
import { PrismaService } from './prisma.service';

@Global()
@Module({
  providers: [PrismaService],
  exports: [PrismaService],
})
export class PrismaModule {}
```

- [ ] **Step 4: Escrever teste de ciclo de vida do `PrismaService` (falha primeiro)**

Criar `backend/src/prisma/prisma.service.spec.ts`:

```typescript
import { Test } from '@nestjs/testing';
import { PrismaService } from './prisma.service';

describe('PrismaService', () => {
  it('conecta ao inicializar e desconecta ao destruir o módulo', async () => {
    const moduleRef = await Test.createTestingModule({
      providers: [PrismaService],
    }).compile();

    const prisma = moduleRef.get(PrismaService);
    const connectSpy = jest.spyOn(prisma, '$connect').mockResolvedValue();
    const disconnectSpy = jest.spyOn(prisma, '$disconnect').mockResolvedValue();

    await moduleRef.init();
    expect(connectSpy).toHaveBeenCalledTimes(1);

    await moduleRef.close();
    expect(disconnectSpy).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 5: Rodar e confirmar que passa**

Run: `npm test -- prisma.service.spec.ts`
Expected: PASS

- [ ] **Step 6: Ligar `ConfigModule` e `PrismaModule` no `AppModule`**

Editar `backend/src/app.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { AppController } from './app.controller';
import { AppService } from './app.service';
import { PrismaModule } from './prisma/prisma.module';

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true, envFilePath: '.env.local' }),
    PrismaModule,
  ],
  controllers: [AppController],
  providers: [AppService],
})
export class AppModule {}
```

- [ ] **Step 7: Habilitar CORS para o frontend em `main.ts`**

Editar `backend/src/main.ts`:

```typescript
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  app.enableCors({ origin: process.env.FRONTEND_ORIGIN ?? 'http://localhost:5173' });
  await app.listen(process.env.PORT ?? 3000);
}
bootstrap();
```

- [ ] **Step 8: Confirmar que a aplicação sobe sem erros**

Run: `npm run start:dev`
Expected: log `Nest application successfully started` sem exceptions de conexão com o Postgres. Parar o processo depois (Ctrl+C).

- [ ] **Step 9: Commit**

```bash
git add backend/src/prisma/prisma.service.ts backend/src/prisma/prisma.module.ts backend/src/prisma/prisma.service.spec.ts backend/src/app.module.ts backend/src/main.ts backend/package.json backend/package-lock.json
git commit -m "feat(backend): PrismaModule global e ConfigModule com .env.local"
```

---

## Task 3: Verificação de JWT via JWKS do Supabase

**Files:**
- Create: `backend/src/auth/jwt-verifier.ts`
- Create: `backend/src/auth/jwt-verifier.spec.ts`
- Create: `backend/src/auth/supabase-jwt-verifier.service.ts`

**Interfaces:**
- Consumes: `ConfigService` (`@nestjs/config`, Task 2) para ler `SUPABASE_URL`.
- Produces: `createJwtVerifier(getKey: JWTVerifyGetKey, issuer: string): (token: string) => Promise<SupabaseJwtPayload>` (função pura, testável sem rede) e `SupabaseJwtVerifier` (serviço Nest com `verifyToken(token: string): Promise<SupabaseJwtPayload>`) — usado pelo `JwtAuthGuard` (Task 4).

- [ ] **Step 1: Instalar `jose`**

```bash
cd backend
npm install jose
```

- [ ] **Step 2: Escrever o teste da função pura de verificação (falha primeiro)**

Criar `backend/src/auth/jwt-verifier.spec.ts`:

```typescript
import { SignJWT, exportJWK, generateKeyPair, createLocalJWKSet, type JWTVerifyGetKey } from 'jose';
import { createJwtVerifier } from './jwt-verifier';

describe('createJwtVerifier', () => {
  const issuer = 'https://test-project.supabase.co/auth/v1';

  async function buildVerifier() {
    const { privateKey, publicKey } = await generateKeyPair('ES256');
    const publicJwk = await exportJWK(publicKey);
    publicJwk.kid = 'test-key';
    publicJwk.alg = 'ES256';
    publicJwk.use = 'sig';

    const getKey: JWTVerifyGetKey = createLocalJWKSet({ keys: [publicJwk] });
    return { privateKey, verify: createJwtVerifier(getKey, issuer) };
  }

  it('aceita um token assinado corretamente, com issuer e audience corretos', async () => {
    const { privateKey, verify } = await buildVerifier();

    const token = await new SignJWT({ email: 'user@example.com', role: 'authenticated' })
      .setProtectedHeader({ alg: 'ES256', kid: 'test-key' })
      .setIssuer(issuer)
      .setAudience('authenticated')
      .setSubject('user-123')
      .setExpirationTime('1h')
      .sign(privateKey);

    const payload = await verify(token);

    expect(payload.sub).toBe('user-123');
    expect(payload.email).toBe('user@example.com');
  });

  it('rejeita um token com issuer diferente', async () => {
    const { privateKey, verify } = await buildVerifier();

    const token = await new SignJWT({ role: 'authenticated' })
      .setProtectedHeader({ alg: 'ES256', kid: 'test-key' })
      .setIssuer('https://outro-projeto.supabase.co/auth/v1')
      .setAudience('authenticated')
      .setSubject('user-123')
      .setExpirationTime('1h')
      .sign(privateKey);

    await expect(verify(token)).rejects.toThrow();
  });

  it('rejeita um token expirado', async () => {
    const { privateKey, verify } = await buildVerifier();

    const agora = Math.floor(Date.now() / 1000);
    const token = await new SignJWT({ role: 'authenticated' })
      .setProtectedHeader({ alg: 'ES256', kid: 'test-key' })
      .setIssuer(issuer)
      .setAudience('authenticated')
      .setSubject('user-123')
      .setIssuedAt(agora - 7200)
      .setExpirationTime(agora - 3600)
      .sign(privateKey);

    await expect(verify(token)).rejects.toThrow();
  });
});
```

- [ ] **Step 3: Rodar e confirmar que falha (a função ainda não existe)**

Run: `npm test -- jwt-verifier.spec.ts`
Expected: FAIL com "Cannot find module './jwt-verifier'"

- [ ] **Step 4: Implementar `createJwtVerifier`**

Criar `backend/src/auth/jwt-verifier.ts`:

```typescript
import { jwtVerify, type JWTVerifyGetKey, type JWTPayload } from 'jose';

export interface SupabaseJwtPayload extends JWTPayload {
  sub: string;
  email?: string;
}

export function createJwtVerifier(getKey: JWTVerifyGetKey, issuer: string) {
  return async function verify(token: string): Promise<SupabaseJwtPayload> {
    const { payload } = await jwtVerify(token, getKey, {
      issuer,
      audience: 'authenticated',
    });
    return payload as SupabaseJwtPayload;
  };
}
```

- [ ] **Step 5: Rodar e confirmar que passa**

Run: `npm test -- jwt-verifier.spec.ts`
Expected: PASS (3 testes)

- [ ] **Step 6: Criar o serviço Nest que usa o JWKS remoto do Supabase**

Criar `backend/src/auth/supabase-jwt-verifier.service.ts`:

```typescript
import { Injectable } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { createRemoteJWKSet } from 'jose';
import { createJwtVerifier, type SupabaseJwtPayload } from './jwt-verifier';

@Injectable()
export class SupabaseJwtVerifier {
  private readonly verify: (token: string) => Promise<SupabaseJwtPayload>;

  constructor(config: ConfigService) {
    const supabaseUrl = config.getOrThrow<string>('SUPABASE_URL');
    const issuer = `${supabaseUrl}/auth/v1`;
    const getKey = createRemoteJWKSet(new URL(`${issuer}/.well-known/jwks.json`));
    this.verify = createJwtVerifier(getKey, issuer);
  }

  verifyToken(token: string): Promise<SupabaseJwtPayload> {
    return this.verify(token);
  }
}
```

- [ ] **Step 7: Commit**

```bash
git add backend/src/auth/jwt-verifier.ts backend/src/auth/jwt-verifier.spec.ts backend/src/auth/supabase-jwt-verifier.service.ts backend/package.json backend/package-lock.json
git commit -m "feat(backend): verificação de JWT do Supabase via JWKS"
```

---

## Task 4: JwtAuthGuard

**Files:**
- Create: `backend/src/auth/jwt-auth.guard.ts`
- Create: `backend/src/auth/jwt-auth.guard.spec.ts`

**Interfaces:**
- Consumes: `SupabaseJwtVerifier.verifyToken` (Task 3).
- Produces: `JwtAuthGuard` (`CanActivate`) — em caso de sucesso define `request.jwtPayload: SupabaseJwtPayload`; usado por `TenantGuard` (Task 5) e por `MeController` (Task 7).

- [ ] **Step 1: Escrever o teste do guard (falha primeiro)**

Criar `backend/src/auth/jwt-auth.guard.spec.ts`:

```typescript
import type { ExecutionContext } from '@nestjs/common';
import { UnauthorizedException } from '@nestjs/common';
import { JwtAuthGuard } from './jwt-auth.guard';
import type { SupabaseJwtVerifier } from './supabase-jwt-verifier.service';

function buildContext(headers: Record<string, string>) {
  const request: Record<string, unknown> = { headers };
  const context = {
    switchToHttp: () => ({ getRequest: () => request }),
  } as unknown as ExecutionContext;
  return { context, request };
}

describe('JwtAuthGuard', () => {
  it('rejeita quando não há cabeçalho Authorization', async () => {
    const verifier = { verifyToken: jest.fn() } as unknown as SupabaseJwtVerifier;
    const guard = new JwtAuthGuard(verifier);
    const { context } = buildContext({});

    await expect(guard.canActivate(context)).rejects.toThrow(UnauthorizedException);
  });

  it('rejeita quando o token é inválido', async () => {
    const verifier = {
      verifyToken: jest.fn().mockRejectedValue(new Error('assinatura inválida')),
    } as unknown as SupabaseJwtVerifier;
    const guard = new JwtAuthGuard(verifier);
    const { context } = buildContext({ authorization: 'Bearer token-invalido' });

    await expect(guard.canActivate(context)).rejects.toThrow(UnauthorizedException);
  });

  it('aceita um token válido e anexa o payload na request', async () => {
    const payload = { sub: 'user-123', email: 'user@example.com' };
    const verifier = {
      verifyToken: jest.fn().mockResolvedValue(payload),
    } as unknown as SupabaseJwtVerifier;
    const guard = new JwtAuthGuard(verifier);
    const { context, request } = buildContext({ authorization: 'Bearer token-valido' });

    const resultado = await guard.canActivate(context);

    expect(resultado).toBe(true);
    expect(request.jwtPayload).toEqual(payload);
    expect(verifier.verifyToken).toHaveBeenCalledWith('token-valido');
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npm test -- jwt-auth.guard.spec.ts`
Expected: FAIL com "Cannot find module './jwt-auth.guard'"

- [ ] **Step 3: Implementar o guard**

Criar `backend/src/auth/jwt-auth.guard.ts`:

```typescript
import { CanActivate, ExecutionContext, Injectable, UnauthorizedException } from '@nestjs/common';
import type { Request } from 'express';
import { SupabaseJwtVerifier } from './supabase-jwt-verifier.service';
import type { SupabaseJwtPayload } from './jwt-verifier';

export type RequestComJwt = Request & { jwtPayload?: SupabaseJwtPayload };

@Injectable()
export class JwtAuthGuard implements CanActivate {
  constructor(private readonly verifier: SupabaseJwtVerifier) {}

  async canActivate(context: ExecutionContext): Promise<boolean> {
    const request = context.switchToHttp().getRequest<RequestComJwt>();
    const header = request.headers.authorization;

    if (!header?.startsWith('Bearer ')) {
      throw new UnauthorizedException('Cabeçalho Authorization ausente ou inválido');
    }

    const token = header.slice('Bearer '.length);

    try {
      request.jwtPayload = await this.verifier.verifyToken(token);
    } catch {
      throw new UnauthorizedException('Token inválido ou expirado');
    }

    return true;
  }
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `npm test -- jwt-auth.guard.spec.ts`
Expected: PASS (3 testes)

- [ ] **Step 5: Commit**

```bash
git add backend/src/auth/jwt-auth.guard.ts backend/src/auth/jwt-auth.guard.spec.ts
git commit -m "feat(backend): JwtAuthGuard valida Bearer token via JWKS"
```

---

## Task 5: TenantContext e TenantGuard

**Files:**
- Create: `backend/src/auth/tenant-context.ts`
- Create: `backend/src/auth/tenant.guard.ts`
- Create: `backend/src/auth/tenant.guard.spec.ts`
- Create: `backend/src/auth/auth.module.ts`

**Interfaces:**
- Consumes: `request.jwtPayload` (Task 4), `PrismaService` (`usuario`, `usuarioEmpresa`) (Task 2).
- Produces: `TenantContextData { usuarioId: string; empresaId: string; perfil: Perfil }`; `TenantContext.get(): TenantContextData` (injetável request-scoped); `TenantGuard` (`CanActivate`) — define `request.tenantContext`; `AuthModule` exportando `SupabaseJwtVerifier`, `JwtAuthGuard`, `TenantGuard`, `TenantContext` — usado por `MeModule` (Task 7).

- [ ] **Step 1: Criar o tipo e o provider `TenantContext`**

Criar `backend/src/auth/tenant-context.ts`:

```typescript
import { Inject, Injectable, Scope } from '@nestjs/common';
import { REQUEST } from '@nestjs/core';
import type { Perfil } from '@prisma/client';
import type { Request } from 'express';

export interface TenantContextData {
  usuarioId: string;
  empresaId: string;
  perfil: Perfil;
}

export type RequestComTenant = Request & { tenantContext?: TenantContextData };

@Injectable({ scope: Scope.REQUEST })
export class TenantContext {
  constructor(@Inject(REQUEST) private readonly request: RequestComTenant) {}

  get(): TenantContextData {
    if (!this.request.tenantContext) {
      throw new Error('TenantContext acessado fora de uma rota protegida por TenantGuard');
    }
    return this.request.tenantContext;
  }
}
```

- [ ] **Step 2: Escrever o teste do `TenantGuard` (falha primeiro)**

Criar `backend/src/auth/tenant.guard.spec.ts`:

```typescript
import type { ExecutionContext } from '@nestjs/common';
import { ForbiddenException, InternalServerErrorException } from '@nestjs/common';
import { TenantGuard } from './tenant.guard';
import type { PrismaService } from '../prisma/prisma.service';
import type { RequestComJwt } from './jwt-auth.guard';

function buildContext(request: Partial<RequestComJwt>) {
  return {
    switchToHttp: () => ({ getRequest: () => request }),
  } as unknown as ExecutionContext;
}

describe('TenantGuard', () => {
  function buildPrismaMock() {
    return {
      usuario: { upsert: jest.fn() },
      usuarioEmpresa: { findMany: jest.fn() },
    } as unknown as PrismaService;
  }

  it('lança erro se o JwtAuthGuard não rodou antes', async () => {
    const prisma = buildPrismaMock();
    const guard = new TenantGuard(prisma);
    const request: Partial<RequestComJwt> = {};

    await expect(guard.canActivate(buildContext(request))).rejects.toThrow(InternalServerErrorException);
  });

  it('rejeita usuário sem empresa associada', async () => {
    const prisma = buildPrismaMock();
    (prisma.usuario.upsert as jest.Mock).mockResolvedValue({ id: 'usuario-1' });
    (prisma.usuarioEmpresa.findMany as jest.Mock).mockResolvedValue([]);
    const guard = new TenantGuard(prisma);
    const request: Partial<RequestComJwt> = { jwtPayload: { sub: 'sub-1', email: 'a@b.com' } };

    await expect(guard.canActivate(buildContext(request))).rejects.toThrow(ForbiddenException);
  });

  it('rejeita usuário com mais de uma empresa (não suportado nesta fase)', async () => {
    const prisma = buildPrismaMock();
    (prisma.usuario.upsert as jest.Mock).mockResolvedValue({ id: 'usuario-1' });
    (prisma.usuarioEmpresa.findMany as jest.Mock).mockResolvedValue([
      { empresaId: 'empresa-1', perfil: 'admin' },
      { empresaId: 'empresa-2', perfil: 'membro' },
    ]);
    const guard = new TenantGuard(prisma);
    const request: Partial<RequestComJwt> = { jwtPayload: { sub: 'sub-1', email: 'a@b.com' } };

    await expect(guard.canActivate(buildContext(request))).rejects.toThrow(InternalServerErrorException);
  });

  it('resolve o tenant e anexa tenantContext na request', async () => {
    const prisma = buildPrismaMock();
    (prisma.usuario.upsert as jest.Mock).mockResolvedValue({ id: 'usuario-1' });
    (prisma.usuarioEmpresa.findMany as jest.Mock).mockResolvedValue([
      { empresaId: 'empresa-1', perfil: 'admin' },
    ]);
    const guard = new TenantGuard(prisma);
    const request: Partial<RequestComJwt> & { tenantContext?: unknown } = {
      jwtPayload: { sub: 'sub-1', email: 'a@b.com' },
    };

    const resultado = await guard.canActivate(buildContext(request));

    expect(resultado).toBe(true);
    expect(request.tenantContext).toEqual({
      usuarioId: 'usuario-1',
      empresaId: 'empresa-1',
      perfil: 'admin',
    });
  });
});
```

- [ ] **Step 3: Rodar e confirmar que falha**

Run: `npm test -- tenant.guard.spec.ts`
Expected: FAIL com "Cannot find module './tenant.guard'"

- [ ] **Step 4: Implementar o `TenantGuard`**

Criar `backend/src/auth/tenant.guard.ts`:

```typescript
import {
  CanActivate,
  ExecutionContext,
  ForbiddenException,
  Injectable,
  InternalServerErrorException,
} from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';
import type { RequestComJwt } from './jwt-auth.guard';
import type { TenantContextData } from './tenant-context';

type RequestComTudo = RequestComJwt & { tenantContext?: TenantContextData };

@Injectable()
export class TenantGuard implements CanActivate {
  constructor(private readonly prisma: PrismaService) {}

  async canActivate(context: ExecutionContext): Promise<boolean> {
    const request = context.switchToHttp().getRequest<RequestComTudo>();
    const payload = request.jwtPayload;

    if (!payload) {
      throw new InternalServerErrorException('TenantGuard exige que JwtAuthGuard rode antes');
    }

    const usuario = await this.prisma.usuario.upsert({
      where: { supabaseUserId: payload.sub },
      update: {},
      create: {
        supabaseUserId: payload.sub,
        nome: payload.email ? payload.email.split('@')[0] : payload.sub,
        email: payload.email ?? `${payload.sub}@sem-email.local`,
      },
    });

    const vinculos = await this.prisma.usuarioEmpresa.findMany({
      where: { usuarioId: usuario.id },
    });

    if (vinculos.length === 0) {
      throw new ForbiddenException('Usuário sem empresa associada');
    }
    if (vinculos.length > 1) {
      throw new InternalServerErrorException(
        'Usuário associado a mais de uma empresa — não suportado nesta fase',
      );
    }

    request.tenantContext = {
      usuarioId: usuario.id,
      empresaId: vinculos[0].empresaId,
      perfil: vinculos[0].perfil,
    };

    return true;
  }
}
```

- [ ] **Step 5: Rodar e confirmar que passa**

Run: `npm test -- tenant.guard.spec.ts`
Expected: PASS (4 testes)

- [ ] **Step 6: Criar o `AuthModule`**

Criar `backend/src/auth/auth.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { SupabaseJwtVerifier } from './supabase-jwt-verifier.service';
import { JwtAuthGuard } from './jwt-auth.guard';
import { TenantGuard } from './tenant.guard';
import { TenantContext } from './tenant-context';

@Module({
  providers: [SupabaseJwtVerifier, JwtAuthGuard, TenantGuard, TenantContext],
  exports: [SupabaseJwtVerifier, JwtAuthGuard, TenantGuard, TenantContext],
})
export class AuthModule {}
```

- [ ] **Step 7: Commit**

```bash
git add backend/src/auth/tenant-context.ts backend/src/auth/tenant.guard.ts backend/src/auth/tenant.guard.spec.ts backend/src/auth/auth.module.ts
git commit -m "feat(backend): TenantGuard resolve empresa/perfil e popula TenantContext"
```

---

## Task 6: AuditService

**Files:**
- Create: `backend/src/audit/audit.service.ts`
- Create: `backend/src/audit/audit.service.spec.ts`
- Create: `backend/src/audit/audit.module.ts`

**Interfaces:**
- Consumes: `PrismaService.auditLog` (Task 2).
- Produces: `AuditService.record(params: RecordAuditParams): Promise<void>`; `AuditModule` exportando `AuditService` — usado por `MeController` (Task 7).

- [ ] **Step 1: Escrever o teste do `AuditService` (falha primeiro)**

Criar `backend/src/audit/audit.service.spec.ts`:

```typescript
import { AuditService } from './audit.service';
import type { PrismaService } from '../prisma/prisma.service';

describe('AuditService', () => {
  it('grava um AuditLog com os dados informados', async () => {
    const create = jest.fn().mockResolvedValue({});
    const prisma = { auditLog: { create } } as unknown as PrismaService;
    const service = new AuditService(prisma);

    await service.record({
      empresaId: 'empresa-1',
      atorUsuarioId: 'usuario-1',
      acao: 'consultar_me',
      dadosDepois: { perfil: 'admin' },
    });

    expect(create).toHaveBeenCalledWith({
      data: {
        empresaId: 'empresa-1',
        atorUsuarioId: 'usuario-1',
        acao: 'consultar_me',
        dadosAntes: undefined,
        dadosDepois: { perfil: 'admin' },
      },
    });
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npm test -- audit.service.spec.ts`
Expected: FAIL com "Cannot find module './audit.service'"

- [ ] **Step 3: Implementar o `AuditService`**

Criar `backend/src/audit/audit.service.ts`:

```typescript
import { Injectable } from '@nestjs/common';
import type { Prisma } from '@prisma/client';
import { PrismaService } from '../prisma/prisma.service';

export interface RecordAuditParams {
  empresaId: string;
  atorUsuarioId: string;
  acao: string;
  dadosAntes?: Prisma.InputJsonValue | null;
  dadosDepois?: Prisma.InputJsonValue | null;
}

@Injectable()
export class AuditService {
  constructor(private readonly prisma: PrismaService) {}

  async record(params: RecordAuditParams): Promise<void> {
    await this.prisma.auditLog.create({
      data: {
        empresaId: params.empresaId,
        atorUsuarioId: params.atorUsuarioId,
        acao: params.acao,
        dadosAntes: params.dadosAntes ?? undefined,
        dadosDepois: params.dadosDepois ?? undefined,
      },
    });
  }
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `npm test -- audit.service.spec.ts`
Expected: PASS

- [ ] **Step 5: Criar o `AuditModule`**

Criar `backend/src/audit/audit.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { AuditService } from './audit.service';

@Module({
  providers: [AuditService],
  exports: [AuditService],
})
export class AuditModule {}
```

- [ ] **Step 6: Commit**

```bash
git add backend/src/audit/audit.service.ts backend/src/audit/audit.service.spec.ts backend/src/audit/audit.module.ts
git commit -m "feat(backend): AuditService grava ExecucaoDeEtapa genérica (AuditLog)"
```

---

## Task 7: MeController (`GET /me`)

**Files:**
- Create: `backend/src/me/me.controller.ts`
- Create: `backend/src/me/me.controller.spec.ts`
- Create: `backend/src/me/me.module.ts`
- Modify: `backend/src/app.module.ts`

**Interfaces:**
- Consumes: `JwtAuthGuard`, `TenantGuard`, `TenantContext.get()` (Task 5), `AuditService.record` (Task 6), `PrismaService.usuario`/`empresa` (Task 2).
- Produces: `GET /me` retornando `{ usuario: { id, nome, email }, empresa: { id, nome }, perfil }` — é o endpoint do caso de validação da Fase 1 (spec §8), consumido pelo frontend na Task 12.

- [ ] **Step 1: Escrever o teste do controller (falha primeiro)**

Criar `backend/src/me/me.controller.spec.ts`:

```typescript
import { MeController } from './me.controller';
import type { TenantContext } from '../auth/tenant-context';
import type { PrismaService } from '../prisma/prisma.service';
import type { AuditService } from '../audit/audit.service';

describe('MeController', () => {
  it('retorna usuário, empresa e perfil, e grava auditoria', async () => {
    const tenantContext = {
      get: () => ({ usuarioId: 'usuario-1', empresaId: 'empresa-1', perfil: 'admin' as const }),
    } as unknown as TenantContext;

    const prisma = {
      usuario: {
        findUniqueOrThrow: jest.fn().mockResolvedValue({
          id: 'usuario-1',
          nome: 'Ana',
          email: 'ana@empresa-a.com',
        }),
      },
      empresa: {
        findUniqueOrThrow: jest.fn().mockResolvedValue({ id: 'empresa-1', nome: 'Empresa A' }),
      },
    } as unknown as PrismaService;

    const audit = { record: jest.fn().mockResolvedValue(undefined) } as unknown as AuditService;

    const controller = new MeController(tenantContext, prisma, audit);

    const resultado = await controller.getMe();

    expect(resultado).toEqual({
      usuario: { id: 'usuario-1', nome: 'Ana', email: 'ana@empresa-a.com' },
      empresa: { id: 'empresa-1', nome: 'Empresa A' },
      perfil: 'admin',
    });
    expect(audit.record).toHaveBeenCalledWith(
      expect.objectContaining({ empresaId: 'empresa-1', atorUsuarioId: 'usuario-1', acao: 'consultar_me' }),
    );
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npm test -- me.controller.spec.ts`
Expected: FAIL com "Cannot find module './me.controller'"

- [ ] **Step 3: Implementar o `MeController`**

Criar `backend/src/me/me.controller.ts`:

```typescript
import { Controller, Get, UseGuards } from '@nestjs/common';
import { JwtAuthGuard } from '../auth/jwt-auth.guard';
import { TenantGuard } from '../auth/tenant.guard';
import { TenantContext } from '../auth/tenant-context';
import { PrismaService } from '../prisma/prisma.service';
import { AuditService } from '../audit/audit.service';

@Controller('me')
@UseGuards(JwtAuthGuard, TenantGuard)
export class MeController {
  constructor(
    private readonly tenantContext: TenantContext,
    private readonly prisma: PrismaService,
    private readonly audit: AuditService,
  ) {}

  @Get()
  async getMe() {
    const { usuarioId, empresaId, perfil } = this.tenantContext.get();

    const usuario = await this.prisma.usuario.findUniqueOrThrow({ where: { id: usuarioId } });
    const empresa = await this.prisma.empresa.findUniqueOrThrow({ where: { id: empresaId } });

    await this.audit.record({
      empresaId,
      atorUsuarioId: usuarioId,
      acao: 'consultar_me',
      dadosDepois: { perfil },
    });

    return {
      usuario: { id: usuario.id, nome: usuario.nome, email: usuario.email },
      empresa: { id: empresa.id, nome: empresa.nome },
      perfil,
    };
  }
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `npm test -- me.controller.spec.ts`
Expected: PASS

- [ ] **Step 5: Criar o `MeModule` e importar no `AppModule`**

Criar `backend/src/me/me.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { MeController } from './me.controller';
import { AuthModule } from '../auth/auth.module';
import { AuditModule } from '../audit/audit.module';

@Module({
  imports: [AuthModule, AuditModule],
  controllers: [MeController],
})
export class MeModule {}
```

Editar `backend/src/app.module.ts` para importar `MeModule`:

```typescript
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { AppController } from './app.controller';
import { AppService } from './app.service';
import { PrismaModule } from './prisma/prisma.module';
import { MeModule } from './me/me.module';

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true, envFilePath: '.env.local' }),
    PrismaModule,
    MeModule,
  ],
  controllers: [AppController],
  providers: [AppService],
})
export class AppModule {}
```

- [ ] **Step 6: Commit**

```bash
git add backend/src/me/me.controller.ts backend/src/me/me.controller.spec.ts backend/src/me/me.module.ts backend/src/app.module.ts
git commit -m "feat(backend): GET /me retorna usuario/empresa/perfil e audita a consulta"
```

---

## Task 8: Helpers de teste do Supabase Admin API

**Files:**
- Create: `backend/src/testing/supabase-admin.helper.ts`
- Create: `backend/src/testing/provision-usuario.helper.ts`

**Interfaces:**
- Consumes: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_PUBLISHABLE_KEY` (env vars).
- Produces: `createTestUser(email, password)`, `deleteTestUser(userId)`, `signInTestUser(email, password): Promise<string>` (access token); `provisionUsuarioParaEmpresa(prisma, params): Promise<Usuario>` — usados pelo seed (Task 9) e pelo e2e (Task 10).

- [ ] **Step 1: Adicionar `SUPABASE_PUBLISHABLE_KEY` ao ambiente do backend**

Em `backend/.env.local`, adicionar (mesmo valor não-secreto já usado em `frontend/.env.local` como `VITE_SUPABASE_PUBLISHABLE_KEY`):

```
SUPABASE_PUBLISHABLE_KEY=<mesmo valor de VITE_SUPABASE_PUBLISHABLE_KEY do frontend>
```

Em `backend/.env.example`, adicionar a mesma variável com um placeholder:

```
SUPABASE_PUBLISHABLE_KEY=your-publishable-key
```

- [ ] **Step 2: Instalar `@supabase/supabase-js` no backend**

```bash
cd backend
npm install @supabase/supabase-js
```

- [ ] **Step 3: Criar o helper de Admin API**

Criar `backend/src/testing/supabase-admin.helper.ts`:

```typescript
import { createClient, type User } from '@supabase/supabase-js';

function requireEnv(name: string): string {
  const value = process.env[name];
  if (!value) {
    throw new Error(`Variável de ambiente ${name} não configurada`);
  }
  return value;
}

function createSupabaseAdminClient() {
  return createClient(requireEnv('SUPABASE_URL'), requireEnv('SUPABASE_SERVICE_ROLE_KEY'), {
    auth: { autoRefreshToken: false, persistSession: false },
  });
}

function createSupabaseAnonClient() {
  return createClient(requireEnv('SUPABASE_URL'), requireEnv('SUPABASE_PUBLISHABLE_KEY'), {
    auth: { autoRefreshToken: false, persistSession: false },
  });
}

export async function createTestUser(email: string, password: string): Promise<User> {
  const admin = createSupabaseAdminClient();
  const { data, error } = await admin.auth.admin.createUser({
    email,
    password,
    email_confirm: true,
  });

  if (error || !data.user) {
    throw new Error(`Falha ao criar usuário de teste ${email}: ${error?.message}`);
  }

  return data.user;
}

export async function deleteTestUser(userId: string): Promise<void> {
  const admin = createSupabaseAdminClient();
  await admin.auth.admin.deleteUser(userId);
}

export async function signInTestUser(email: string, password: string): Promise<string> {
  const anon = createSupabaseAnonClient();
  const { data, error } = await anon.auth.signInWithPassword({ email, password });

  if (error || !data.session) {
    throw new Error(`Falha ao logar usuário de teste ${email}: ${error?.message}`);
  }

  return data.session.access_token;
}
```

- [ ] **Step 4: Criar o helper de provisionamento de usuário/empresa**

Criar `backend/src/testing/provision-usuario.helper.ts`:

```typescript
import type { Perfil, PrismaClient, Usuario } from '@prisma/client';

export interface ProvisionarUsuarioParams {
  supabaseUserId: string;
  nome: string;
  email: string;
  empresaId: string;
  perfil: Perfil;
}

export async function provisionUsuarioParaEmpresa(
  prisma: PrismaClient,
  params: ProvisionarUsuarioParams,
): Promise<Usuario> {
  const usuario = await prisma.usuario.upsert({
    where: { supabaseUserId: params.supabaseUserId },
    update: {},
    create: {
      supabaseUserId: params.supabaseUserId,
      nome: params.nome,
      email: params.email,
    },
  });

  await prisma.usuarioEmpresa.upsert({
    where: { usuarioId_empresaId: { usuarioId: usuario.id, empresaId: params.empresaId } },
    update: { perfil: params.perfil },
    create: { usuarioId: usuario.id, empresaId: params.empresaId, perfil: params.perfil },
  });

  return usuario;
}
```

- [ ] **Step 5: Commit**

```bash
git add backend/src/testing/supabase-admin.helper.ts backend/src/testing/provision-usuario.helper.ts backend/.env.example backend/package.json backend/package-lock.json
git commit -m "feat(backend): helpers de teste para Supabase Admin API e provisionamento de tenant"
```

Nota: `backend/.env.local` não entra no commit (está no `.gitignore`); atualize-o manualmente com o valor real de `SUPABASE_PUBLISHABLE_KEY`.

---

## Task 9: Script de seed

**Files:**
- Create: `backend/prisma/seed.ts`

**Interfaces:**
- Consumes: `createTestUser` (Task 8), `provisionUsuarioParaEmpresa` (Task 8), `PrismaClient` (Task 1).
- Produces: dados de seed executáveis via `npm run db:seed` — 2 empresas, 1 usuário admin em cada, usado para desenvolvimento manual e como referência para o e2e (Task 10).

- [ ] **Step 1: Implementar o seed**

Criar `backend/prisma/seed.ts`:

```typescript
import { PrismaClient } from '@prisma/client';
import { createTestUser } from '../src/testing/supabase-admin.helper';
import { provisionUsuarioParaEmpresa } from '../src/testing/provision-usuario.helper';

const prisma = new PrismaClient();

async function encontrarOuCriarEmpresa(nome: string) {
  const existente = await prisma.empresa.findFirst({ where: { nome } });
  if (existente) return existente;
  return prisma.empresa.create({ data: { nome } });
}

async function seedEmpresaComAdmin(nomeEmpresa: string, email: string, password: string) {
  const empresa = await encontrarOuCriarEmpresa(nomeEmpresa);
  const authUser = await createTestUser(email, password);

  await provisionUsuarioParaEmpresa(prisma, {
    supabaseUserId: authUser.id,
    nome: email.split('@')[0],
    email,
    empresaId: empresa.id,
    perfil: 'admin',
  });

  console.log(`Seed OK: ${nomeEmpresa} <- ${email}`);
}

async function main() {
  await seedEmpresaComAdmin('Empresa Seed A', 'seed-a@corepilot.dev', 'Seed123!456');
  await seedEmpresaComAdmin('Empresa Seed B', 'seed-b@corepilot.dev', 'Seed123!456');
}

main()
  .catch((err) => {
    console.error(err);
    process.exit(1);
  })
  .finally(() => prisma.$disconnect());
```

- [ ] **Step 2: Rodar o seed**

```bash
npm run db:seed
```

Expected: duas linhas `Seed OK: ...` no console. Se falhar com "User already registered", os usuários já existem de uma execução anterior — pode ignorar (script já é idempotente para os vínculos de empresa).

- [ ] **Step 3: Commit**

```bash
git add backend/prisma/seed.ts
git commit -m "feat(backend): seed com 2 empresas e usuários admin para teste de isolamento"
```

---

## Task 10: E2E — isolamento multi-tenant em `GET /me`

**Files:**
- Create: `backend/test/me.e2e-spec.ts`

**Interfaces:**
- Consumes: `AppModule` (Task 7), `createTestUser`/`deleteTestUser`/`signInTestUser` (Task 8), `PrismaService`.
- Produces: prova automatizada do caso de validação da Fase 1 (spec §8) — login real, `GET /me` correto por empresa, isolamento entre tenants, auditoria gravada.

- [ ] **Step 1: Escrever o teste e2e**

Criar `backend/test/me.e2e-spec.ts`:

```typescript
import { INestApplication } from '@nestjs/common';
import { Test } from '@nestjs/testing';
import * as request from 'supertest';
import { AppModule } from '../src/app.module';
import { PrismaService } from '../src/prisma/prisma.service';
import { createTestUser, deleteTestUser, signInTestUser } from '../src/testing/supabase-admin.helper';
import { provisionUsuarioParaEmpresa } from '../src/testing/provision-usuario.helper';

describe('GET /me (isolamento multi-tenant)', () => {
  let app: INestApplication;
  let prisma: PrismaService;
  const authUserIdsParaLimpar: string[] = [];
  const empresaIdsParaLimpar: string[] = [];

  beforeAll(async () => {
    const moduleRef = await Test.createTestingModule({ imports: [AppModule] }).compile();
    app = moduleRef.createNestApplication();
    await app.init();
    prisma = app.get(PrismaService);
  });

  afterAll(async () => {
    await prisma.auditLog.deleteMany({ where: { empresaId: { in: empresaIdsParaLimpar } } });
    await prisma.usuarioEmpresa.deleteMany({ where: { empresaId: { in: empresaIdsParaLimpar } } });
    await prisma.usuario.deleteMany({
      where: { supabaseUserId: { in: authUserIdsParaLimpar } },
    });
    await prisma.empresa.deleteMany({ where: { id: { in: empresaIdsParaLimpar } } });

    for (const userId of authUserIdsParaLimpar) {
      await deleteTestUser(userId);
    }

    await app.close();
  });

  async function criarEmpresaComUsuarioLogado(nomeEmpresa: string, email: string) {
    const empresa = await prisma.empresa.create({ data: { nome: nomeEmpresa } });
    empresaIdsParaLimpar.push(empresa.id);

    const password = 'TestePhase1!23';
    const authUser = await createTestUser(email, password);
    authUserIdsParaLimpar.push(authUser.id);

    await provisionUsuarioParaEmpresa(prisma, {
      supabaseUserId: authUser.id,
      nome: email.split('@')[0],
      email,
      empresaId: empresa.id,
      perfil: 'admin',
    });

    const accessToken = await signInTestUser(email, password);

    return { empresa, accessToken };
  }

  it('retorna a empresa correta para cada usuário e nunca mistura dados entre tenants', async () => {
    const sufixo = Date.now();
    const empresaA = await criarEmpresaComUsuarioLogado('E2E Empresa A', `e2e-a-${sufixo}@corepilot.dev`);
    const empresaB = await criarEmpresaComUsuarioLogado('E2E Empresa B', `e2e-b-${sufixo}@corepilot.dev`);

    const respostaA = await request(app.getHttpServer())
      .get('/me')
      .set('Authorization', `Bearer ${empresaA.accessToken}`)
      .expect(200);

    const respostaB = await request(app.getHttpServer())
      .get('/me')
      .set('Authorization', `Bearer ${empresaB.accessToken}`)
      .expect(200);

    expect(respostaA.body.empresa.id).toBe(empresaA.empresa.id);
    expect(respostaB.body.empresa.id).toBe(empresaB.empresa.id);
    expect(respostaA.body.empresa.id).not.toBe(respostaB.body.empresa.id);

    const logsA = await prisma.auditLog.findMany({ where: { empresaId: empresaA.empresa.id } });
    const logsB = await prisma.auditLog.findMany({ where: { empresaId: empresaB.empresa.id } });
    expect(logsA).toHaveLength(1);
    expect(logsB).toHaveLength(1);
    expect(logsA[0].acao).toBe('consultar_me');
  });

  it('rejeita requisição sem token', async () => {
    await request(app.getHttpServer()).get('/me').expect(401);
  });
});
```

- [ ] **Step 2: Rodar e confirmar que passa**

Run: `npm run test:e2e -- me.e2e-spec.ts`
Expected: PASS (2 testes). Se falhar por timeout, aumentar o timeout do Jest e2e (`jest.setTimeout(20000)` no topo do arquivo) — chamadas reais à API do Supabase podem ser mais lentas que testes locais.

- [ ] **Step 3: Commit**

```bash
git add backend/test/me.e2e-spec.ts
git commit -m "test(backend): e2e cobre isolamento multi-tenant real de GET /me"
```

---

## Task 11: Frontend — sessão e login

**Files:**
- Create: `frontend/src/corepilot/auth/useSession.ts`
- Create: `frontend/src/corepilot/auth/LoginForm.tsx`
- Create: `frontend/src/corepilot/api/apiFetch.ts`
- Modify: `frontend/.env.example`, `frontend/.env.local`

**Interfaces:**
- Consumes: `supabase` client (`frontend/src/lib/supabase/client.ts`, já existente).
- Produces: `useSession(): { session: Session | null; loading: boolean }`; `<LoginForm />`; `apiFetch(path, accessToken, init?): Promise<Response>` — usados por `AuthGate`/`FundacaoStatus` (Task 12).

- [ ] **Step 1: Adicionar `VITE_API_BASE_URL` ao ambiente do frontend**

Em `frontend/.env.example`, adicionar:

```
VITE_API_BASE_URL=http://localhost:3000
```

Em `frontend/.env.local`, adicionar a mesma linha (valor local, não é segredo).

- [ ] **Step 2: Criar o hook de sessão**

Criar `frontend/src/corepilot/auth/useSession.ts`:

```typescript
import { useEffect, useState } from 'react';
import type { Session } from '@supabase/supabase-js';
import { supabase } from '../../lib/supabase/client';

export interface SessionState {
  session: Session | null;
  loading: boolean;
}

export function useSession(): SessionState {
  const [session, setSession] = useState<Session | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    supabase.auth.getSession().then(({ data }) => {
      setSession(data.session);
      setLoading(false);
    });

    const { data: listener } = supabase.auth.onAuthStateChange((_event, newSession) => {
      setSession(newSession);
    });

    return () => listener.subscription.unsubscribe();
  }, []);

  return { session, loading };
}
```

- [ ] **Step 3: Criar o helper de chamada à API**

Criar `frontend/src/corepilot/api/apiFetch.ts`:

```typescript
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL as string | undefined;

export async function apiFetch(path: string, accessToken: string, init?: RequestInit): Promise<Response> {
  if (!API_BASE_URL) {
    throw new Error('Missing VITE_API_BASE_URL. Configure it in .env.local.');
  }

  return fetch(`${API_BASE_URL}${path}`, {
    ...init,
    headers: {
      ...init?.headers,
      Authorization: `Bearer ${accessToken}`,
    },
  });
}
```

- [ ] **Step 4: Criar o formulário de login**

Criar `frontend/src/corepilot/auth/LoginForm.tsx`:

```typescript
import { useState, type FormEvent } from 'react';
import { supabase } from '../../lib/supabase/client';

export function LoginForm() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState<string | null>(null);
  const [submitting, setSubmitting] = useState(false);

  async function handleSubmit(event: FormEvent) {
    event.preventDefault();
    setSubmitting(true);
    setError(null);

    const { error: signInError } = await supabase.auth.signInWithPassword({ email, password });

    if (signInError) {
      setError(signInError.message);
    }

    setSubmitting(false);
  }

  return (
    <form
      onSubmit={handleSubmit}
      style={{ maxWidth: 320, margin: '80px auto', display: 'flex', flexDirection: 'column', gap: 12 }}
    >
      <h1>CorePilot</h1>
      <input
        type="email"
        placeholder="E-mail"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        required
      />
      <input
        type="password"
        placeholder="Senha"
        value={password}
        onChange={(e) => setPassword(e.target.value)}
        required
      />
      {error && <div style={{ color: 'crimson', fontSize: 13 }}>{error}</div>}
      <button type="submit" disabled={submitting}>
        {submitting ? 'Entrando…' : 'Entrar'}
      </button>
    </form>
  );
}
```

- [ ] **Step 5: Verificação manual (sem test runner no frontend ainda)**

```bash
cd frontend
npm run dev
```

Abrir o navegador em `http://localhost:5173`, confirmar que o formulário de login aparece (a tela ainda não reage ao submit — isso é resolvido na Task 12, que consome `useSession`). Parar o servidor depois (Ctrl+C).

- [ ] **Step 6: Commit**

```bash
git add frontend/src/corepilot/auth/useSession.ts frontend/src/corepilot/auth/LoginForm.tsx frontend/src/corepilot/api/apiFetch.ts frontend/.env.example
git commit -m "feat(frontend): hook de sessão Supabase, formulário de login e cliente de API"
```

Nota: `frontend/.env.local` não entra no commit (está no `.gitignore`).

---

## Task 12: Frontend — AuthGate, FundacaoStatus e wiring em App.tsx

**Files:**
- Create: `frontend/src/corepilot/auth/FundacaoStatus.tsx`
- Create: `frontend/src/corepilot/auth/AuthGate.tsx`
- Modify: `frontend/src/App.tsx`

**Interfaces:**
- Consumes: `useSession` (Task 11), `apiFetch` (Task 11), `GET /me` response shape (Task 7: `{ usuario, empresa, perfil }`), `CorePilotApp` (existente, `frontend/src/corepilot/CorePilotApp.tsx`).
- Produces: fluxo completo logout→login→`/me`→protótipo, satisfazendo o caso de validação da Fase 1 na UI.

- [ ] **Step 1: Criar a tela de status pós-login**

Criar `frontend/src/corepilot/auth/FundacaoStatus.tsx`:

```typescript
import { useEffect, useState } from 'react';
import type { Session } from '@supabase/supabase-js';
import { supabase } from '../../lib/supabase/client';
import { apiFetch } from '../api/apiFetch';
import { CorePilotApp } from '../CorePilotApp';

interface MeResponse {
  usuario: { id: string; nome: string; email: string };
  empresa: { id: string; nome: string };
  perfil: string;
}

export function FundacaoStatus({ session }: { session: Session }) {
  const [me, setMe] = useState<MeResponse | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [showPrototype, setShowPrototype] = useState(false);

  useEffect(() => {
    let cancelado = false;

    apiFetch('/me', session.access_token)
      .then(async (response) => {
        if (!response.ok) {
          throw new Error(`GET /me falhou com status ${response.status}`);
        }
        const data = (await response.json()) as MeResponse;
        if (!cancelado) setMe(data);
      })
      .catch((err: Error) => {
        if (!cancelado) setError(err.message);
      });

    return () => {
      cancelado = true;
    };
  }, [session.access_token]);

  if (showPrototype) {
    return <CorePilotApp />;
  }

  return (
    <div style={{ maxWidth: 480, margin: '80px auto', display: 'flex', flexDirection: 'column', gap: 16 }}>
      <h1>CorePilot — Fundação</h1>
      {error && <div style={{ color: 'crimson' }}>{error}</div>}
      {!error && !me && <div>Carregando /me…</div>}
      {me && (
        <div style={{ border: '1px solid #ddd', borderRadius: 8, padding: 16 }}>
          <div>
            <strong>Usuário:</strong> {me.usuario.nome} ({me.usuario.email})
          </div>
          <div>
            <strong>Empresa:</strong> {me.empresa.nome}
          </div>
          <div>
            <strong>Perfil:</strong> {me.perfil}
          </div>
        </div>
      )}
      <div style={{ display: 'flex', gap: 12 }}>
        <button onClick={() => setShowPrototype(true)}>Ver protótipo (mock)</button>
        <button onClick={() => supabase.auth.signOut()}>Sair</button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Criar o `AuthGate`**

Criar `frontend/src/corepilot/auth/AuthGate.tsx`:

```typescript
import { useSession } from './useSession';
import { LoginForm } from './LoginForm';
import { FundacaoStatus } from './FundacaoStatus';

export function AuthGate() {
  const { session, loading } = useSession();

  if (loading) return <div style={{ padding: 40 }}>Carregando…</div>;
  if (!session) return <LoginForm />;
  return <FundacaoStatus session={session} />;
}
```

- [ ] **Step 3: Ligar `AuthGate` em `App.tsx`**

Editar `frontend/src/App.tsx`:

```typescript
import { AuthGate } from './corepilot/auth/AuthGate';

function App() {
  return <AuthGate />;
}

export default App;
```

- [ ] **Step 4: Verificação manual completa**

Com o backend rodando (`cd backend && npm run start:dev`) e o seed já executado (Task 9):

```bash
cd frontend
npm run dev
```

No navegador (`http://localhost:5173`):
1. Fazer login com `seed-a@corepilot.dev` / `Seed123!456`.
2. Confirmar que a tela mostra `Empresa Seed A` e perfil `admin`.
3. Clicar em "Sair", logar com `seed-b@corepilot.dev` / `Seed123!456`.
4. Confirmar que a tela mostra `Empresa Seed B` (nunca dados da Empresa A).
5. Clicar em "Ver protótipo (mock)" e confirmar que o `CorePilotApp` navegável ainda abre normalmente.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/corepilot/auth/FundacaoStatus.tsx frontend/src/corepilot/auth/AuthGate.tsx frontend/src/App.tsx
git commit -m "feat(frontend): AuthGate liga login real, status de tenant e protótipo mock"
```

---

## Task 13: Verificação final do caso de validação (spec §8)

**Files:** nenhum arquivo novo — checklist de verificação.

- [ ] **Step 1: Rodar toda a suíte do backend**

```bash
cd backend
npm test
npm run test:e2e
```

Expected: todos os testes PASS (unit + e2e, incluindo o smoke test do Prisma e o e2e de isolamento multi-tenant).

- [ ] **Step 2: Confirmar auditoria via Prisma Studio**

```bash
npm run prisma:studio
```

Abrir a tabela `AuditLog` e confirmar que existem registros com `acao = 'consultar_me'` para as chamadas feitas manualmente na Task 12.

- [ ] **Step 3: Checklist final contra a spec (§8)**

- [ ] Login real via Supabase Auth funciona no frontend.
- [ ] `GET /me` retorna dados corretos por empresa.
- [ ] Dois usuários de empresas diferentes nunca veem dado um do outro (validado manualmente na Task 12 e automaticamente na Task 10).
- [ ] Toda chamada a `/me` gera uma linha em `AuditLog`.
- [ ] Nenhum segredo (chave Anthropic ainda não se aplica; `DATABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`) está commitado no repositório (`git status` não deve listar `backend/.env.local`).

- [ ] **Step 4: Confirmar que nada sensível foi commitado**

```bash
git log --stat -- backend/.env.local
```

Expected: nenhum resultado (o arquivo nunca deve aparecer no histórico do git).

Se todos os itens acima passarem, a Fase 1 (Fundação) está validada e a Fase 2 (Módulo + Chat) pode começar como um novo ciclo brainstorm → spec → plano.
